<UserControl x:Class="Schedule.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Schedule.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel HorizontalAlignment="Center">
            <TextBlock Text="Головне меню" FontSize="25" Margin="5"/>
            <Button Content="Робота з вчителями"
                    Command="{Binding RedirectToTeachersEditViewCommand}"/>
            <Button Content="Робота з класами" 
                    Command="{Binding RedirectToClassesEditViewCommand}"/>
            <Button Content="Робота з предметами"/>
            <Button Content="Робота з аудиторіями"/>
            <Button Content="Робота з часовими слотами"/>
            <Button Content="Робота з розкладом"/>
        </StackPanel>
    </Grid>
    
</UserControl>
